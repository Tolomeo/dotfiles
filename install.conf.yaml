- defaults:
    link:
      relink: true

- clean: ["~"]

- create:
    - ~/.config

- link:
    ~/.zshrc: zsh/.zshrc
    ~/.config/nvim: nvim

- shell:
    - [git submodule update --init --recursive, Installing submodules]
