- defaults:
    link:
      relink: true

- clean: ["~"]

- create:
    - ~/.config

- link:
    ~/.zshrc: .zshrc
    ~/.config/nvim: nvim

- shell:
    - [git submodule update --init --recursive, Installing submodules]
